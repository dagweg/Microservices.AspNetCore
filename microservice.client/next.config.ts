import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "c1.neweggimages.com",
      "wapcomputer.com",
      "cdn11.bigcommerce.com",
      "www.costco.com",
      "hk-media.apjonlinecdn.com",
    ],
  },
};

export default nextConfig;
